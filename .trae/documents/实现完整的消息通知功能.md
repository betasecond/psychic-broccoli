# 实现完整的消息通知功能

## 1. 后端实现

### 1.1 数据库设计
- 在 `backend/database/schema.sql` 中添加消息相关表
  - `messages` 表：存储用户消息
  - `notifications` 表：存储系统通知
  - `discussions` 表：存储课程讨论

### 1.2 模型定义
- 在 `backend/models/models.go` 中添加消息相关模型定义

### 1.3 API实现
- 在 `backend/handlers/` 中添加 `messages.go` 文件，实现消息相关API：
  - 获取消息列表
  - 标记消息已读/未读
  - 删除消息
  - 获取通知列表
  - 获取课程讨论列表

### 1.4 路由配置
- 在 `backend/main.go` 中添加消息相关路由

## 2. 前端实现

### 2.1 服务层
- 在 `frontend/src/services/` 中添加 `messageService.ts`，封装消息相关API调用

### 2.2 状态管理
- 在 `frontend/src/store/slices/` 中添加 `messageSlice.ts`，管理消息状态

### 2.3 组件集成
- 修改 `frontend/src/pages/student/MessagesPage.tsx`：
  - 将模拟数据替换为真实API调用
  - 集成Redux状态管理
  - 实现完整的消息操作功能

## 3. 测试与验证

### 3.1 后端测试
- 运行后端服务，测试API接口

### 3.2 前端测试
- 运行前端开发服务器，测试消息功能
- 验证标记已读/未读、删除等功能正常工作

## 4. 代码质量检查

- 确保代码符合项目的代码风格
- 运行前端和后端的测试套件
- 修复所有编译错误和警告

## 5. 提交与推送

- 将所有修改添加到Git暂存区
- 提交代码，添加详细的提交信息
- 推送代码到远程仓库

这个计划将确保我们实现完整的消息通知功能，包括后端数据存储、API接口和前端交互，并在功能实现后进行代码质量检查和版本控制操作。