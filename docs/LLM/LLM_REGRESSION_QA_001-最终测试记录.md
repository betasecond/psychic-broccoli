# LLM_REGRESSION_QA_001 最终测试记录

日期：2026-04-28

## 任务目标

执行最终回归，验证以下内容：

- 后端测试执行情况
- 前端构建执行情况
- RAG 问答能力无回归
- 手动章节/课时管理无回归
- 手动新增/编辑题目无回归
- 汇总当前未覆盖风险

## 自动化验证结果

### 1. 后端全量测试

执行命令：

```powershell
docker run --rm -v "${PWD}:/app" -w /app/backend golang:1.24.9 sh -lc "/usr/local/go/bin/go mod tidy && /usr/local/go/bin/go test ./..."
```

结果：通过

结论：

- 后端全量测试已恢复为绿灯
- `go mod tidy` 之后，`backend/go.mod` 新增了直接依赖 `golang.org/x/time v0.12.0`
- 当前工作区里 `backend/go.sum` 无新增 diff，说明校验项原本已齐

### 2. 前端构建

执行命令：

```powershell
npm run build
```

结果：通过

补充说明：

- Vite 生产构建成功输出 `dist/`
- 构建过程中仅出现 chunk size warning，不影响本任务验收

## 真实接口回归验证

### 验证环境

- 现有本地联调后端：`http://127.0.0.1:18080`
- 为 RAG 成功路径额外启动的隔离后端：`http://127.0.0.1:18081`
- 隔离后端镜像：`psychic-backend:local`
- 隔离数据库副本：`backend/database/education.regression-ragqa-20260428.db`
- 本地 mock LLM provider：`127.0.0.1:19090`

说明：

- 隔离后端仅用于最终回归验证，不影响用户当前查看的 `18080` 联调服务
- mock provider 提供固定的 `/embeddings` 与 `/chat/completions` 响应，用于验证 RAG 上传、检索、生成、历史写入整链路

### 1. RAG 问答回归

验证步骤：

1. 使用教师账号 `fengranran / password123` 登录隔离后端
2. 创建临时课程
3. 上传文本知识库文档 `ai-guide.txt`
4. 查询知识库文档列表
5. 调用 `POST /courses/:id/rag/query`
6. 查询 `GET /courses/:id/rag/queries`
7. 删除临时课程，清理验证数据

验证结果：

- 文档上传成功：是
- 文档列表可读取：是
- RAG 问答成功返回答案：是
- RAG 返回至少 1 条来源分块：是
- 问答历史成功持久化：是

结论：

- RAG 上传、检索、生成、历史记录链路未被本轮 LLM 解析相关改动破坏

### 2. 手动章节/课时管理回归

验证步骤：

1. 创建临时课程
2. 手动创建章节
3. 手动创建课时
4. 更新章节标题
5. 更新课时标题与内容
6. 查询章节与课时详情
7. 删除临时课程，清理验证数据

验证结果：

- 手动新增章节成功：是
- 手动新增课时成功：是
- 手动更新章节成功：是
- 手动更新课时成功：是
- 查询结果与更新后数据一致：是

结论：

- 课程编辑页原有“手动章节/课时管理”能力无回归

### 3. 手动新增/编辑题目回归

验证步骤：

1. 基于临时课程创建临时考试
2. 手动新增 1 道单选题
3. 手动更新题干、分值
4. 查询考试详情与题目列表
5. 删除临时考试与临时课程，清理验证数据

验证结果：

- 手动新增题目成功：是
- 手动更新题目成功：是
- 考试详情读取成功：是
- 查询结果与更新后题目数据一致：是

结论：

- 教师端原有“手动新增/编辑题目”能力无回归

## 最终验收结论

本任务验收项结论如下：

1. 后端测试通过，或记录明确失败原因：满足
2. 前端构建通过，或记录明确失败原因：满足
3. 既有 RAG 问答、手动课程管理、手动题目管理无回归：满足

## 变更摘要

本轮没有新增业务代码实现，主要完成了最终回归验证、依赖收敛与记录沉淀：

- 执行 `go mod tidy`，消除后端全量测试阻塞
- 复跑后端全量 `go test ./...` 并通过
- 完成后端依赖状态收敛
- 复核前端生产构建状态
- 使用隔离 mock provider 验证 RAG 成功链路
- 使用真实接口验证手动章节/课时管理与手动题目管理未被破坏

## 未覆盖风险清单

当前仍有以下未完全覆盖或需后续关注的风险：

1. RAG 成功链路本轮使用的是本地 mock provider，不代表真实第三方模型服务在网络抖动、限流、模型变更时的行为。
2. 最终回归主要通过 API 级验证完成，未对教师端完整页面交互做一轮逐页人工点击回归。
3. 本轮真实验证基于数据库副本与临时数据，未覆盖线上长期积累数据下的边缘内容形态。
